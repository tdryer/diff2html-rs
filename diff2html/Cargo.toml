[package]
name = "diff2html"
version = "0.1.0"
edition = "2024"
description = "Parse unified diffs and generate HTML"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
thiserror = "2.0"
once_cell = "1.19"
handlebars = "6.2"
